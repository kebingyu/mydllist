// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mydllist", "for mydllist support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mydllist", "enable mydllist support", "no");

if (PHP_MYDLLIST != "no") {
	EXTENSION("mydllist", "mydllist.c");
}

